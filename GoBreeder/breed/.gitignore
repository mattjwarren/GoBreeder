# Runtime-generated files - not tracked in git

# Genome written per-generation by breeder.py for each match
breeding_genome.py

# Debug: genome currently executing in vm.py
vm_running_genome.py

# Live population and rolling snapshots (written by breeder.py each generation)
current_population.py
current_population.py_save
current_population.py_save_stats

# Run log (append-only, written by all modules)
runlog.txt

# Optional per-move history tree
histories/

# Python cache
__pycache__/
*.pyc
